# Network Address Translation (NAT) Lab

This project demonstrates the configuration of Network Address Translation (NAT) and default routing using Cisco Packet Tracer. Multiple private networks were connected to an external network through a router acting as a gateway. The lab focuses on NAT implementation, IP addressing, default route configuration, and end-to-end connectivity verification, providing practical experience with key CCNA routing and network access concepts.
